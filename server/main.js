import {Meteor} from "meteor/meteor";
import {Players} from "./../imports/api/players";

Meteor.startup( () => {
  // Players.insert({
  //   name: "Lui",
  //   score: 20
  // });
  // let square =function (x, y) {
  //   return x * x
  // }

  // let square = (x, y) => x * x;

  // console.log(square(10));

  // let user = {
  //   name: "Gernot",
  //   sayHi () {
  //     setTimeout(() => {
  //       console.log(this.name);
  //     },1000);
  //   }
  // };
  //
  // user.sayHi(1, 2)

  // let numbers = [9, 99, 4, 56];
  // let newNumbers = numbers.map((numbers) => numbers += 1);
  //
  // console.log(newNumbers);
});
